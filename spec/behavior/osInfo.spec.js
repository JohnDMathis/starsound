var osInfo = require( "../../resource/app/osInfo.js" );
describe.only( "osInfo", function() {
	describe( 'getting ip address', function() {
		before( function() {
			debugger;
			var ip = osInfo.getIpAddress();
		} );

	} );
} );